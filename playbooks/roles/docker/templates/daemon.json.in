{
  "storage-driver": "{{ docker_storage_driver }}" 
  {% if docker_storage_driver == 'overlay2' and docker_package == 'docker-ee' %}
  ,
  "storage-opts": [
    "overlay2.override_kernel_check=true"
  ]
  {% endif %}
}